<?xml version="1.0" encoding="euc-kr"?>
<query>
	<type>select</type>
	<description><![CDATA[]]></description>
	<reload>true</reload>
	<monitoring>true</monitoring>
	<result-count>1</result-count>
	<statement>
<![CDATA[
SELECT A.RECIPE_CD, A.RECIPE_NAME,
        B.ITEM_CODE, (SELECT ITEM_NAME FROM HLDC_PO_ITEM_MST WHERE ITEM_CODE=B.ITEM_CODE) ITEM_NAME,
        C.DISUSE_RATE,B.NEED_QTY,B.ITEM_SEQ,
       --ROUND((CAL * B.NEED_QTY / 100),2) "열량",
       ROUND( NVL(C.CAL,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "열량",
       --ROUND((WATER * B.NEED_QTY / 100),2) "수분",
       ROUND( NVL(C.WATER,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "수분",
       --ROUND((PROT * B.NEED_QTY / 100),2) "단백질",
       ROUND( NVL(C.PROT,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "단백질",
       --ROUND((FAT * B.NEED_QTY / 100),2) "지질",
       ROUND( NVL(C.FAT,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "지질",
       --ROUND((CARBO * B.NEED_QTY / 100),2) "당질",
       ROUND( NVL(C.CARBO,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "당질",
       --ROUND((FIBER * B.NEED_QTY / 100),2) "섬유",
       ROUND( NVL(C.FIBER,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "섬유",
       --ROUND((ASH * B.NEED_QTY / 100),2) "회분",
       ROUND( NVL(C.ASH,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "회분",
       --ROUND((CA * B.NEED_QTY / 100),2) "칼슘",
       ROUND( NVL(C.CA,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "칼슘",
       --ROUND((P * B.NEED_QTY / 100),2) "인",
       ROUND( NVL(C.P,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "인",
       --ROUND((FE * B.NEED_QTY / 100),2) "철",
       ROUND( NVL(C.FE,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "철",
       --ROUND((NA * B.NEED_QTY / 100),2) "나트륨",
       ROUND( NVL(C.NA,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "나트륨",
       --ROUND((K * B.NEED_QTY / 100),2) "칼륨",
       ROUND( NVL(C.K,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "칼륨",
       --ROUND((VITA_A * B.NEED_QTY / 100),2) "비타민A",
       ROUND( NVL(C.VITA_A,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "비타민A",
       --ROUND((RETIN * B.NEED_QTY / 100),2) "레티놀",
       ROUND( NVL(C.RETIN,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "레티놀",
       --ROUND((B_CAROT * B.NEED_QTY / 100),2) "B_카르틴",
       ROUND( NVL(C.B_CAROT,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "B_카르틴",
       --ROUND((THIA * B.NEED_QTY / 100),2) "비타민B1",
       ROUND( NVL(C.THIA,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "비타민B1",
       --ROUND((RIBO * B.NEED_QTY / 100),2) "비타민B2",
       ROUND( NVL(C.RIBO,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "비타민B2",
       --ROUND((NIACIN * B.NEED_QTY / 100),2) "나이아신",
       ROUND( NVL(C.NIACIN,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "나이아신",
       --ROUND((VITA_C * B.NEED_QTY / 100),2) "비타민C",
       ROUND( NVL(C.VITA_C,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "비타민C",
       --ROUND((CHOLE * B.NEED_QTY / 100),2) "콜레스테롤"
       ROUND( NVL(C.CHOLE,0) * DECODE( NVL(C.DISUSE_RATE,0), 0, B.NEED_QTY, ROUND(B.NEED_QTY*(100-C.DISUSE_RATE)/100,2) ) / 100,2) "콜레스테롤"
  FROM FSR_STD_RECIPE_MST A ,
       FSR_STD_RECIPE_ITEM B ,
       FSI_ITEM_NUT C
 WHERE A.RECIPE_CD = B.RECIPE_CD
   AND B.ITEM_CODE = C.ITEM_CODE
   AND A.RECIPE_CD = #RECIPE_CD#
ORDER BY A.RECIPE_CD,B.ITEM_SEQ	]]></statement>
	<input default-name="ds_Nut">
		<col name="RECIPE_CD" size="20" type="VARCHAR" description="" /> 
	</input>
	<output default-name="ds_NutList">
		<col name="RECIPE_CD" size="20" type="VARCHAR" description="" /> 
		<col name="RECIPE_NAME" size="50" type="VARCHAR" description="" /> 
		<col name="ITEM_CODE" size="12" type="VARCHAR" description="" /> 
		<col name="ITEM_NAME" size="200" type="VARCHAR" description="" /> 
		<col name="DISUSE_RATE" size="22" type="NUMERIC" description="" /> 
		<col name="NEED_QTY" size="22" type="NUMERIC" description="" /> 
		<col name="ITEM_SEQ" size="22" type="NUMERIC" description="" /> 
		<col name="열량" size="22" type="NUMERIC" description="" /> 
		<col name="수분" size="22" type="NUMERIC" description="" /> 
		<col name="단백질" size="22" type="NUMERIC" description="" /> 
		<col name="지질" size="22" type="NUMERIC" description="" /> 
		<col name="당질" size="22" type="NUMERIC" description="" /> 
		<col name="섬유" size="22" type="NUMERIC" description="" /> 
		<col name="회분" size="22" type="NUMERIC" description="" /> 
		<col name="칼슘" size="22" type="NUMERIC" description="" /> 
		<col name="인" size="22" type="NUMERIC" description="" /> 
		<col name="철" size="22" type="NUMERIC" description="" /> 
		<col name="나트륨" size="22" type="NUMERIC" description="" /> 
		<col name="칼륨" size="22" type="NUMERIC" description="" /> 
		<col name="비타민A" size="22" type="NUMERIC" description="" /> 
		<col name="레티놀" size="22" type="NUMERIC" description="" /> 
		<col name="B_카르틴" size="22" type="NUMERIC" description="" /> 
		<col name="비타민B1" size="22" type="NUMERIC" description="" /> 
		<col name="비타민B2" size="22" type="NUMERIC" description="" /> 
		<col name="나이아신" size="22" type="NUMERIC" description="" /> 
		<col name="비타민C" size="22" type="NUMERIC" description="" /> 
		<col name="콜레스테롤" size="22" type="NUMERIC" description="" /> 
	</output>
</query>
